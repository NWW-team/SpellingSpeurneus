# De echte woordenlijst wordt per run opgehaald bij OpenTaal (enkele MB's).
# Die hoort niet in de repository.
data/woordenlijst.txt

# Python-restanten
__pycache__/
*.pyc

# Het crawlresultaat gaat naar Supabase, niet in deze publieke repository.
resultaten.json
